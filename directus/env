# General Configuration
APP_NAME="directus-demo"
APP_ENV=staging
DIRECTUS_PORT=8055
APP_DEBUG=false

PUBLIC_URL= "https://domain.com"
PUBLIC_HOST= "domain.com"
COMPOSE_PROJECT_NAME="${APP_NAME}_directus_${APP_ENV}" # for docker composer, we need this env

# Database configuration for postgres
POSTGRES_PORT=5432
POSTGRES_USER=directus_user
POSTGRES_PASSWORD=directus_password123
POSTGRES_DB=directus_db
POSTGRES_EXPOSE_PORT=5437

# Database Configuration for directus
DB_HOST=database
DB_PORT=${POSTGRES_PORT}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_DATABASE=${POSTGRES_DB}

# Security
SECRET="SOME_SECRET_KEY"
REFRESH_TOKEN_COOKIE_NAME="directus_refresh_token"
KEY="KEY"
PASSWORD_RESET_URL_ALLOW_LIST= "https://domain.com/reset-password"

# SMTP CONFIGURATION
EMAIL_FROM=""
EMAIL_FROM_NAME=""
EMAIL_REPLY_TO_NAME=""
EMAIL_REPLY_TO="reply@example.com"
EMAIL_TRANSPORT="smtp"

# EMAIL_SMTP_POOL=false
EMAIL_SMTP_HOST="mail.domain.com"
EMAIL_SMTP_PORT=465
EMAIL_SMTP_SECURE=true

# EMAIL_SMTP_IGNORE_TLS=false
EMAIL_SMTP_USER="smtp user"
EMAIL_SMTP_PASSWORD=""


# REDIS FOR DIRECTUS
CACHE_STORE= "redis"
REDIS_HOST_PASSWORD="redispassword123"
REDIS= "redis://cache:6379?password=$REDIS_HOST_PASSWORD"

ADMIN_EMAIL= "admin@domain.com"
ADMIN_PASSWORD= "d1r3ctu5"

PM2_INSTANCES=2

# DIRECTUS DATA RETENSION
RETENTION_ENABLED=true
RETENTION_BATCH=500
ACTIVITY_RETENTION=5d
REVISIONS_RETENTION=5d
FLOW_LOGS_RETENTION=5d
RETENTION_SCHEDULE="0 0 * * *"
WEBSOCKETS_ENABLED=true

## DIRECTUS CONFIGURATIONS
## Related to Cache
CACHE_ENABLED=true
CACHE_TTL=2h
CACHE_AUTO_PURGE=true
CACHE_AUTO_PURGE_IGNORE_LIST=directus_activity,directus_presets
SYNCHRONIZATION_STORE=redis